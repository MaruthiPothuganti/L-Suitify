import { ACTION_TYPE } from "../Utils/constants";
const filterReducer = (state, action) => {
    const { CATEGORIES,
        PRODUCTS,
        PRICE,
        RATING,
        SORT_BY,
        FAST_DELIVERY,
        STOCK,
        STATUS,
        HIGH_TO_LOW,
        LOW_TO_HIGH,
        CLEAR
    } = ACTION_TYPE;

}


export { filterReducer };
